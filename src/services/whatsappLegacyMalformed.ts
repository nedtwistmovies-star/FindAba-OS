// Deprecated malformed legacy file — renamed as part of normalization pass.
// Original path contained invalid characters and had a committed secret. The secret was removed.
// KEEP FOR MIGRATION: Update any imports referencing the old malformed path to import from
// 'src/services/whatsappLegacyMalformed' instead, then remove this file.

// Export the legacy raw content (sanitized) for now.
export const LEGACY_WHATSAPP_SANITIZED = true;
