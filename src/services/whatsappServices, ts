// WARNING: This file previously contained a hard-coded WhatsApp access token.
// Replaced with a no-secret shim that delegates to ENV-backed service.

export async function sendWhatsAppLegacyShim() {
  // Legacy file preserved as a shim during normalization.
  // It intentionally does not contain any secrets or make external calls.
  return { success: false, error: 'deprecated-legacy-file' };
}
