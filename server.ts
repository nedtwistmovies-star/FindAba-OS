/**
 * Main Express server entry point for FindAba City OS.
 * Imports and executes the modular server application from ./server/index.ts.
 */
import app from "./server/index";

export { app };
export default app;
